name: Diagnose kworld365 jobkoreaglobal

on:
  workflow_dispatch:

jobs:
  diag:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: pip install requests -q
      - name: Diagnose
        run: |
          echo "Runner IP info:"
          curl -s https://api.ipify.org || true
          echo
          python scripts/diag_sites.py
