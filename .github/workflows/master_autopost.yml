name: "MASTER 27-site publisher — DISABLED during clean rebuild"

# All legacy setup/post/cleanup write paths are preserved in git history.
# The rebuilt system only permits audit -> draft -> editorial approval.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Legacy 27-site publishing is locked."
          echo "Use WP Network Rebuild in audit mode first."


