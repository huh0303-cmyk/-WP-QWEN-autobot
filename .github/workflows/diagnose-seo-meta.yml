name: Diagnose SEO Meta (일회성 수동 실행)

on:
  workflow_dispatch:

jobs:
  diagnose:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install requests
      - name: Run diagnosis
        continue-on-error: true
        env:
          KHEALTH365COM: ${{ secrets.KHEALTH365COM }}
        run: python diagnose_seo_meta.py > diagnose_seo_meta_results.txt 2>&1
      - name: Commit results
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add diagnose_seo_meta_results.txt
          git diff --staged --quiet || git commit -m "chore: SEO 메타 진단 결과 [skip ci]"
          git push
