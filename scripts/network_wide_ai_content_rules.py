"""
네트워크 전체(27개 블로그 사이트 + 21개 유튜브 채널) 공통 AI 콘텐츠 생성 규칙
(2026-08-22 사용자 지시: "유튜브도 마찬가지야.. 너가 딱 메모리에 기억해 두고
도와줘 Github박제")

배경: 2026-08-22 하루 동안 블로그 27개 사이트 파이프라인(autopost_mega.py)을
전면 개편하면서 반복적으로 드러난 문제 패턴들 — 이 패턴들은 블로그에만
해당하는 게 아니라 "AI가 대량으로 콘텐츠를 만드는 모든 파이프라인"에 공통
적용되는 문제라서, 유튜브 21개 채널(situation_room_daily.py, curio_longform.py,
health_longform.py, topik_quiz_*.py, youtube_video_pipeline.py,
health_clinic/src/*.py, archive_footage_longform.py, nasa_archive_longform.py,
classic_reads_longform.py, ambient_noise_video.py 등) 쪽에도 동일하게
점검·적용해야 한다.

이 파일은 blog의 content_pipeline_rules.py와 같은 역할(규칙 문서 + 단일
소스)이지만 범위가 두 파이프라인 전체다. 실제 로직은 각 스크립트에 있고,
여기는 "무엇을 반드시 지켜야 하는지"를 고정해두는 문서다.

⚠️ 2026-08-22 기준 상태: 아래 규칙은 블로그(autopost_mega.py)에는 전부
적용 완료했지만, 유튜브 21개 채널 쪽 스크립트들은 **아직 이 기준으로
전수 감사하지 않았다.** 다음에 유튜브 파이프라인을 만지게 되면, 뭔가
고치기 전에 먼저 이 규칙들 기준으로 감사부터 하고 시작할 것 — "고치고
나서 보고"가 아니라 "감사 결과 먼저 보고, 확인받고 고치기".
"""

# 1. AI 제공자 라우팅 ------------------------------------------------------
#
# 원칙: 돈이 드는 생성(텍스트/이미지/검색리서치)은 OpenAI(GPT)가 항상 1순위,
# Gemini는 OPENAI_API_KEY가 아예 없을 때만 쓰는 최후 폴백. Gemini를 "무료
# 대안"으로 나란히 두지 말 것 — 2026-08-02 Gemini 월 지출한도 초과로 하루 종일
# 파이프라인이 막힌 사고, 그리고 2026-08-22 keyword refresh 스크립트가 Gemini
# 검색그라운딩 하나에만 의존했다가 똑같이 막힌 사고가 반복됐다. 새로 뭔가
# 만들 때 "Gemini로 검색/생성한다"는 게 유일한 경로가 되지 않게 항상 GPT
# 경로부터 확보할 것. (블로그 쪽 참고 구현: openai_text.py, autopost_mega.py의
# generate_content_gemini/get_fallback_nanobanana_image,
# refresh_keyword_pool.py의 call_search_llm)
AI_PROVIDER_PRIORITY = "openai_first_gemini_last_resort_only"

# 2. 강제로 채우지 않기 ------------------------------------------------------
#
# 원칙: 이미지든 상품링크든 관련 콘텐츠든, 진짜로 관련 있는 게 없으면 그냥
# 없이 간다. 억지로 무관한 걸로 채우면 "이미지가 없는 것보다 더 나쁘다"(사용자
# 반복 피드백). 유튜브 쪽에서도 썸네일/배경음악/자료화면 등을 못 찾았을 때
# 무관한 걸로 채우지 말고 없이 가거나 스킵할 것.
NO_FORCED_UNRELATED_FILLER = True

# 3. 다양성 — 매번 똑같은 값은 그 자체로 AI 대량생산 흔적 -------------------
#
# 원칙: 글자수, 이미지 개수, 태그 개수, 문장 리듬(문체), 제목 패턴처럼 "매
# 콘텐츠마다 정확히 똑같은 값/구조"로 나가는 게 있으면 그 자체가 신호가 된다.
# 유튜브 쪽도 점검 대상: 인트로 길이, 영상 길이, 썸네일 레이아웃, 나레이션
# 문장 리듬, 자막 스타일이 채널마다/영상마다 기계적으로 똑같지 않은지.
# (블로그 참고: min_chars/이미지장수/태그개수 랜덤화, 사이트별 문장리듬
# 5종 고정배정 — autopost_mega.py의 _RHYTHM_PROFILES/resolve_min_chars)
DIVERSITY_REQUIRED = True

# 4. 콘텐츠 중복 방지는 "자기 자신"이 아니라 "네트워크 전체" 기준 ------------
#
# 원칙: 새 콘텐츠(글/영상)를 만들기 전에 그 사이트/채널 자신의 과거 이력뿐
# 아니라, 같은 네트워크의 인접 주제 다른 사이트/채널 이력과도 겹치는지
# 확인할 것. 내부적으로 새로 만든 것들끼리만 중복체크하고 실제 발행/업로드
# 이력과 대조 안 하면 반드시 겹친다(2026-08-22 실사고 — koreainvest365
# 신규 키워드 50개 중 19개가 이미 발행된 글과 겹쳤음, 재검증 후 수정).
CROSS_NETWORK_DEDUP_REQUIRED = True

# 5. 운영자 개인정보 노출 금지 -----------------------------------------------
#
# 원칙: 27개 사이트가 "서로 다른 운영자"처럼 보이게 설계해놨는데(각기 다른
# 필자명/이메일), 실제로는 WP 관리자 계정 표시이름이 운영자 개인 이메일
# 그대로 공개 노출되고 있었다(25/26 사이트, 2026-08-22 발견·수정). 유튜브
# 쪽도 채널 정보/설명/댓글 등에 운영자 개인 식별정보(실명, 개인 이메일,
# 전화번호 등)가 실수로 노출되고 있지 않은지 점검할 것.
NO_OPERATOR_PII_IN_PUBLIC_OUTPUT = True

# 6. "된다"고 말하기 전에 실제로 확인 -----------------------------------------
#
# 원칙: 코드를 고치고 나서 "이제 될 겁니다"라고 말하지 말 것 — 실제로 돌려서
# 로그/결과를 확인한 다음에만 "됩니다"라고 말할 것. 그리고 문제를 발견하면
# 사용자가 묻기 전에 먼저 보고할 것(같은 부류의 문제가 다른 곳에도 있을
# 가능성까지 포함해서). 2026-08-22 하루에만 이 원칙을 어겨서 사용자가 두 번
# 화를 냄 — 반복 금지.
VERIFY_BEFORE_CLAIMING_SUCCESS = True

# 7. 법적 고지 문구는 코드가 강제 삽입, AI 프롬프트 지시에만 맡기지 않기 ------
#
# 원칙: 의학 디스클레이머, 뉴스 출처표기, (예정된) 쿠팡파트너스 대가성 고지처럼
# 법적/정책적으로 반드시 있어야 하는 문구는 프롬프트 지시만으론 매번 빠지거나
# 표현이 달라질 수 있어서, 코드가 고정 문구를 확정 삽입하는 방식으로 강제한다.
# 쿠팡파트너스 고지는 2026-08-22 확인 기준 본문 "최상단"에 눈에 띄게 있어야
# 하는 걸로 공정위 규정이 바뀌어 있음(예전엔 하단도 허용) — 실제 적용 시
# 이 최신 규정 기준으로 위치를 다시 확인할 것.
LEGAL_TEXT_MUST_BE_CODE_ENFORCED = True
