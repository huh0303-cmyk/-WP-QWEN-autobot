"""
건강채널 장편(8분 내외) 영상 파이프라인 — 고정 규칙 (2026-08-02 사용자 지시로 확정).

이 파일은 향후 만들 health_longform.py 생성 스크립트가 반드시 import해서
지켜야 하는 규칙의 단일 소스다. 매번 대화에서 다시 설명하지 않아도 되도록
여기에 고정해둔다 ("혹실히 박제해놔야해.. 또 잊었다 하지 말고").

채널 정체성: 병증상별 올바른 대처법 + 추천 음식/영양소 소개.
채널의 핵심 차별점은 "추천 음식과 영양소"다 — 단순 증상 설명에 그치지 않고
어떤 음식/영양소가 왜 도움이 되는지까지 짚어주는 게 강점이므로, 채널
소개글이나 마케팅 카피에서는 이 강점을 앞세운다.

"병원이 아닙니다" 같은 부정적 디스클레이머 문구는 채널 설명(마케팅 카피)에는
쓰지 않는다 (2026-08-03 사용자 피드백: "이런말 불필요"). 다만 영상 스크립트의
disclaimer 비트(REQUIRED_SCRIPT_SECTIONS 참고)는 유튜브 건강정보 정책 리스크
때문에 유지 — 이건 마케팅 카피가 아니라 영상 내용 자체의 정책 준수용이라
별개로 취급한다.
"""

# 대본 규칙 ------------------------------------------------------------

# 모든 대본은 반드시 "추천 음식" 섹션을 포함해야 한다.
# 증상 설명 + 대처법만 있고 음식 추천이 빠진 대본은 미완성으로 간주한다.
FOOD_SECTION_REQUIRED = True

# 대본 필수 구성 순서 (언어별로 순서/구체 예시는 달라져도 되지만 아래 4개 파트는
# 빠짐없이 들어가야 한다 — 구글 중복 콘텐츠 판정 회피를 위해 파트 순서 자체는
# 언어 버전마다 조금씩 바꿔도 됨).
REQUIRED_SCRIPT_SECTIONS = [
    "hook_intro",       # 10초 이내 이탈 방지용 후킹
    "symptom_explain",  # 증상/원인 설명
    "coping_methods",   # 올바른 대처법 (병원 진단/치료 아님)
    "food_recommend",   # 추천 음식 (필수, 생략 불가)
    "disclaimer",        # "병원 대체 아님" 안내
]

# 썸네일 규칙 ------------------------------------------------------------

# 통증 부위 사진(정면 최대 크기)에 더해, 추천 음식/채소/과일 이미지를
# 반드시 함께 배치한다 (작은 원형 인서트 등 — 통증 사진의 "정면 최대" 룰은 유지).
THUMBNAIL_MUST_INCLUDE_FOOD_IMAGE = True

# 언어별로 실제 보여주는 음식은 달라야 한다 (중복 콘텐츠 회피 + 문화적 친숙도).
THUMBNAIL_FOOD_EXAMPLES_BY_LANG = {
    "ko": ["고등어", "멸치", "미역", "브로콜리", "견과류"],
    "en": ["salmon", "kale", "bone broth", "walnuts", "blueberries"],
    "ja": ["納豆", "だし昆布", "焼き鮭", "ブロッコリー", "くるみ"],
}

# 주제/중복 콘텐츠 전략 --------------------------------------------------

# 초기 단계(채널 자리 잡을 때까지)에는 3개 언어(ko/en/ja)가 같은 주제를 쓴다.
# 나라별로 실제 인기 검색 질병이 다르다는 건 알지만, 지금은 안정적으로 자리
# 잡는 게 우선이라 주제를 나라별로 쪼개지 않는다 (2026-08-02 사용자 결정).
# 채널이 자리 잡으면 TOPIC_STRATEGY를 "per_country_unique_topic"으로 바꾸고
# 나라별 인기 검색 키워드 기반으로 주제 자체를 분리한다.
TOPIC_STRATEGY = "shared_topic_same_phase"  # 다음 단계: "per_country_unique_topic"

# 현재 파일럿 언어 (지금 만들고 있는 버전).
PILOT_LANGUAGES = ["ko", "en", "ja"]

# 파일럿이 자리 잡으면 추가할 언어 — 나라별 별도 채널("TV")로 확장.
# 비용이 들어도 진행하기로 함 (2026-08-02 사용자 결정: "돈이 들더라도.. 그 나라를
# 겨냥한 별도 TV를 할 수 있어"). 순서는 확정 아님, 진행 시 시장 크기/경쟁도로 재조정 가능.
# 새 언어 추가 시 유튜브 채널부터 별도로 만들어야 함 (채널 생성은 사용자만 가능).
FUTURE_EXPANSION_LANGUAGES = ["es", "fr", "de", "it"]

# 같은 주제를 쓰더라도 구글이 중복/스케일 콘텐츠로 보지 않을 최소 조건.
# 나레이션(대본)은 언어별로 이미 완전히 다르게 쓰고 있으므로, 이미지까지
# 언어별로 전부 새로 만들 필요는 없다 — 같은 이미지 세트를 써도 되지만
# 등장 순서는 언어 버전마다 섞어서 다르게 배치한다. 썸네일은 예외 없이
# 사진/카피/폰트/색상 전부 완전히 다르게 간다 (기존 규칙 그대로 유지).
SAME_TOPIC_DIFFERENTIATION_RULES = {
    "narration": "always_fully_distinct_per_language",   # 번역 아니고 새로 집필
    "image_set": "may_reuse_but_shuffle_order_per_language",  # 이미지 재사용 가능, 순서만 변경
    "thumbnail": "always_fully_distinct",  # 사진/카피/폰트/색상 전부 다르게 (예외 없음)
}

# 2026-08-18: Veo 인트로클립 완전 제거(사용자 지시 — 비용 문제). 이 스크립트는
# 어떤 워크플로우에도 연결돼있지 않은 미사용 프로토타입이지만, "Veo 완전 차단"
# 지시를 코드 전체에 일관되게 반영하기 위해 여기도 정리함. 앞으로 인트로클립이
# 필요하면 curio_longform.py처럼 정지이미지(Ken Burns) 기반으로 만들 것.

# 발행 규칙 ------------------------------------------------------------

# 주당 목표 업로드 빈도 (요일 고정 아님, 대략 3회/주로 페이싱).
PUBLISH_TIMES_PER_WEEK = 3

# 유튜브 업로드는 항상 비공개로만 올라가고, 사용자가 최종 검수 후 직접
# "공개"로 전환한다. 예약 발행(publishAt)도 사용하지 않는다.
# (기존 social_publish.py도 privacyStatus="private"로 강제 중 — 동일 원칙 적용)
YOUTUBE_PRIVACY_STATUS = "private"
AUTO_PUBLISH_ALLOWED = False
